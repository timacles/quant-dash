#+TITLE: Quant Dash 

* Main 
** Theme View Card
   - [ ] leader/laggards
   - [ ] theme/industry/sector
*** theme SQL view 
** Design
 - Status bar on top:
   - date
   - data inconsisntecies
 - navigation
** Split Sections Out 
- add a section loader or just move them to separate pages
- move the Macro section to the bottom;
  
** add data inconsitency alert to page
- make the script return a flag if any critical inconsistencies are found

** Design
- add a status bar with
  - current time,
  - latest data date
  - data inconsistency indicator (

** JSON export 4 analysis
 - LLM analysis
 - start with Macro table
 - the specific signals table

** Theme Dashboard
- leaders/laggards by theme

* Later
** industry leader stocks
- scrape from finviz
- by volume descending

** ETF discrepancies endpoint
** =pull_stats/= endpoint
- displays last pulled
** JSON export to LLM analysis
   - import json -> DB from llm analysis
   - =vw_llm_market_analysis=
   #+begin_src sql
       select jsonb_pretty(summary) 
       from public.vw_llm_market_summary 
       where date ='2026-04-08';
   #+end_src
   - insert LLM analysis into DB
#+begin_src sql
insert into etf_analysis (data)
select pg_read_file('/etc/postgresql/17/main/macro_analysis_20260405_232651.json')::jsonb;
#+end_src

** add graph visualizations
** Range of numbers coloring
or top 3 get highlighted
** ETF metadata editor
** FRED API ?
